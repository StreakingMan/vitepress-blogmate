/**
 * @type {import('vitepress').UserConfig}
 */
const config = {
    themeConfig: {
        todo: 'todo',
        siteTitle: 'Default Site Title',
        search: {
            provider: 'local',
        },
    },
};

module.exports = config;
